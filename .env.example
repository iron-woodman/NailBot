BOT_TOKEN=YOUR_BOT_TOKEN
ADMIN_ID=YOUR_ADMIN_ID
DATABASE_URL=sqlite+aiosqlite:///database/nails.db
TIMEZONE=Europe/Moscow
GOOGLE_CALENDAR_URL=YOUR_GOOGLE_CALENDAR_URL
# Origin, с которого открывается MiniApp (для CORS)
MINIAPP_ORIGIN=https://web.telegram.org
# SQL_ECHO=1 включает логирование всех SQL-запросов (только для отладки:
# в логи попадают имена и username клиентов)
